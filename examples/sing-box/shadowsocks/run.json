{
  // Shadowsocks example for sing-box
  // SERVER, PORT, SOCKS_PORT inherited from ../../run.json
  "name": "Shadowsocks (sing-box)",
  "core": "sing-box",
  "server_config": "server.json",
  "client_config": "client.json",

  "vars": [
    {
      "TITLE": "chacha20",
      // PASSWORD inherited from ../../run.json if set, otherwise "auto"
    },
  ],

  "checks": ["dns", "http", "quic"],
  "optional_checks": ["quic"],
  "timeout_sec": 30,
}
