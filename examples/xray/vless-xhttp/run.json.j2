{
  // VLESS + xHTTP — variants auto-generated via run.json.j2 (Jinja2/Pongo2 template)
  // |split:"," creates a list from a comma-separated string for iteration.
  "name": "VLESS + xHTTP (xray)",
  "server_config": "server",
  "client_config": "client",
  "tls": true,

  // NOTE: xtls-rprx-vision flow is NOT valid over xHTTP — it requires raw
  // TCP+TLS. See the vless-vision example for that. xHTTP uses flow "".
  "vars": [
    {
      "TITLE":      "plain-tls",
      "TLS":        "1",
      "VLESS_ENC":  "none",
      "VLESS_DEC":  "none",
      "VLESS_FLOW": "",
      "HOST_NAME":  "127.0.0.1",
      "SNI_NAME":   "127.0.0.1",
    },
  ],

  "checks": ["dns", "http", "quic", "speedtest"],
  "optional_checks": ["quic"],
  "timeout_sec": 30,
}
